<Page
    x:Class="FxTools.App.Pages.StringToolsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Padding="28" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <StackPanel Spacing="4">
            <TextBlock Style="{StaticResource PageTitleStyle}" Text="字符串处理" />
            <TextBlock Foreground="{ThemeResource AppMutedBrush}" Text="批量清理空白并转换字符大小写。" />
        </StackPanel>
        <Grid Grid.Row="1" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="190" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <ComboBox x:Name="OperationBox" Header="操作" SelectedIndex="0" />
            <Button Grid.Column="1" VerticalAlignment="Bottom" Click="Transform_Click">
                <StackPanel Orientation="Horizontal" Spacing="7">
                    <SymbolIcon Symbol="Edit" />
                    <TextBlock Text="处理" />
                </StackPanel>
            </Button>
            <Button Grid.Column="3" VerticalAlignment="Bottom" Click="Copy_Click" ToolTipService.ToolTip="复制结果">
                <SymbolIcon Symbol="Copy" />
            </Button>
            <Button Grid.Column="4" VerticalAlignment="Bottom" Click="Clear_Click" ToolTipService.ToolTip="清空">
                <SymbolIcon Symbol="Delete" />
            </Button>
        </Grid>
        <Grid Grid.Row="2" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBox x:Name="InputBox" Header="输入" AcceptsReturn="True" TextWrapping="Wrap"
                     ScrollViewer.VerticalScrollBarVisibility="Auto" PlaceholderText="输入或粘贴需要处理的文本" />
            <TextBox x:Name="OutputBox" Grid.Column="1" Header="结果" IsReadOnly="True"
                     AcceptsReturn="True" TextWrapping="Wrap" ScrollViewer.VerticalScrollBarVisibility="Auto" />
        </Grid>
        <TextBlock x:Name="StatusText" Grid.Row="3" Foreground="{ThemeResource AppMutedBrush}" Text="等待输入" />
    </Grid>
</Page>
