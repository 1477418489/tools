# 网络质量监控中心实施计划

## 1. 目标

将现有“UDP 稳定性”模块升级为统一的“网络质量”工作区，在一个有界、可长期运行的会话中完成：

- UDP/STUN、TCP Connect、TLS Handshake 节点质量检测。
- Windows 系统路由、SOCKS5 代理、HTTP CONNECT 代理检测。
- 系统路由与显式代理的同节点对比。
- RTT、峰值、抖动、探测失败率、连续失败、质量等级和趋势监控。
- UDP 公网地址/端口映射与会话内映射变化检测。
- 节点、协议、出口和错误信息的统一明细。

## 2. 网络语义

- “系统路由”表示应用不显式配置代理，套接字交由 Windows 路由表发送。它仍可能经过路由器透明代理、VPN、WFP 或 TUN，不能宣称为物理直连。
- “显式代理”表示应用主动连接用户配置的 SOCKS5 或 HTTP CONNECT 代理。
- 每项结果显示本地源 IP 和网卡；显式代理结果同时显示代理端点，用可验证信息解释实际出口。
- SOCKS5 支持 TCP、TLS 和 UDP ASSOCIATE；HTTP CONNECT 只支持 TCP 和 TLS。
- SOCKS5 和 HTTP CONNECT 使用目标域名发给代理，避免应用侧预先解析造成 DNS 泄漏；系统路由模式由本机解析。
- 普通 STUN Binding 只能观察公网映射，不能单独判定完整 NAT 类型。
- TCP 指标测量连接建立；TLS 指标测量 TCP、代理隧道和 TLS 握手的端到端耗时。
- TLS 使用系统默认信任库、SNI 和 HTTPS 主机名校验，不提供跳过证书校验开关。

## 3. 功能范围

### 3.1 探测目标

每个目标包含：名称、协议、主机、端口、启用状态。默认配置提供：

- 3 个 UDP/STUN 公网节点。
- 2 个 TCP 443 节点。
- 2 个 TLS 443 节点。

最多保存 10 个目标，禁止重复的“协议 + 主机 + 端口”。

### 3.2 出口模式

- 仅系统路由。
- 仅显式代理。
- 系统路由 / 显式代理对比。

代理配置包含类型、主机、端口、可选用户名和会话密码。类型、地址、端口和用户名可保存；密码只保留到当前应用进程结束，不写入配置文件、不写日志、不进入错误文本。

### 3.3 会话结果

- 总体质量、会话时长、平均 RTT、探测失败率、有效监控项。
- 每个“目标 + 出口”的质量、RTT、抖动、成功/失败、连续失败、映射、映射变化和最近错误。
- 选择任意监控项查看最近样本趋势。
- 对比模式显示同一目标的系统路由与代理 RTT 差值。
- HTTP CONNECT 遇到 UDP 目标时显示“不支持”，不计入探测失败率。

## 4. 技术设计

### 4.1 协议层

- 保留并复用 RFC 5389 STUN 编解码。
- 新增 SOCKS5 握手、用户名/密码认证、CONNECT 和 UDP ASSOCIATE 编解码。
- 新增 HTTP CONNECT Basic 认证和有界响应头解析，响应上限 16 KiB。
- 新增直接 TCP 连接探测。
- 新增基于现有 TCP/代理隧道的 TLS 握手探测。

### 4.2 调度与资源

- 未点击开始时不创建线程、不轮询网络。
- 1 个调度线程，最多 6 个工作线程，使用显式有界任务队列。
- 每个监控项最多 1 个在途请求，执行队列上界等于监控项数量。
- 最多 10 个基础目标；对比模式最多产生 20 个监控项。
- 探测间隔最小 1 秒、默认 2 秒；响应超时最大 5 秒。
- 每个监控项最多保留 180 个样本。
- UDP 会话复用数据套接字；SOCKS5 UDP 同时复用控制连接和中继信息。
- TCP/TLS 每次探测建立新连接并使用 try-with-resources 关闭。
- 停止、重置和模块清理必须关闭套接字、代理控制连接和执行器。
- JavaFX 更新只保留最新快照，避免 runLater 队列增长。

### 4.3 配置

- 使用新的当前基线文件 `network_quality_targets.json`。
- 使用 `network_quality_settings.json` 保存不含密码的出口设置。
- 不读取或迁移旧 UDP 配置，不增加兼容分支。
- 严格校验 JSON 字段、类型、长度、端口、数量和重复项。
- 使用项目现有原子写入工具保存。

### 4.4 界面

- 侧栏模块改名为“网络质量”。
- 顶部使用紧凑的目标编辑区、出口模式和代理设置。
- 指标区保持固定高度，不因动态值改变布局。
- 趋势图和监控表占据主要空间；最小窗口下表格滚动。
- 通过协议和出口标记快速区分 UDP/TCP/TLS、系统路由/代理。
- 密码字段使用 PasswordField，离开应用时主动清空控制器引用。

## 5. 测试计划

- STUN IPv4/IPv6、事务校验、错误响应和有界属性解析测试。
- 本机伪 STUN 服务验证直接 UDP。
- 本机伪 SOCKS5 服务验证认证、CONNECT、UDP ASSOCIATE和 UDP 封装。
- 本机伪 HTTP CONNECT 服务验证成功、认证失败、非 2xx 和响应头上限。
- 本机 TCP 服务验证连接探测；TLS 验证失败路径由本机服务覆盖，成功路径使用公共 TLS 冒烟验证。
- 统计质量、连续失败、映射变化、样本上限和对比差值测试。
- 停止在途请求、重复开始/停止、监听器异常和资源释放测试。
- 配置往返、损坏保护、严格基线格式和密码不落盘测试。
- FXML 注入、动作处理器、导航资源和最小尺寸布局回归测试。
- 真实 Windows 系统路由 UDP/TCP/TLS 冒烟验证。
- 如本机存在可用代理，则进行真实代理验证；否则使用本机协议服务器覆盖代理路径。

## 6. 执行顺序

1. 审查并修订本计划的协议可行性、语义、安全性和资源边界。
2. 建立统一模型、配置存储和统计累加器。
3. 实现直接 UDP/TCP/TLS 探测。
4. 实现 SOCKS5 CONNECT、UDP ASSOCIATE 和 HTTP CONNECT。
5. 实现统一会话调度、对比模式和清理。
6. 重构控制器、FXML、导航和浅色主题。
7. 完成单元、集成、真实网络和界面验证。
8. 最终 Review；发现问题直接修改并重复完整构建。

## 7. 验收标准

- 用户能明确看到当前检测使用“系统路由”还是指定代理。
- 同一节点可以同时显示系统路由和代理结果，且不混淆统计。
- SOCKS5 下 UDP、TCP、TLS 可工作；HTTP CONNECT 下 TCP、TLS 可工作。
- 不支持的组合明确显示且不污染失败率。
- 长时间运行不存在无界集合、无界任务堆积、空闲轮询或未关闭套接字。
- 密码不落盘，错误与日志不泄露认证信息。
- 完整测试与 `javafx:jlink` 通过，实际界面无重叠、截断或空白图表异常。

## 8. 计划审查结论

初稿的总体架构可行，但已针对以下问题完成修订：

- 语义修订：TCP/TLS 没有可观测的数据包丢失，统一改称“探测失败率”；UDP/STUN 行仍可解释为响应丢失。
- 出口可解释性：仅使用“直连”标签无法证明物理路径，增加本地源 IP、网卡和代理端点，界面使用“系统路由”这一准确名称。
- DNS 与 TLS 安全：代理模式将域名交给代理解析；TLS 强制系统信任库、SNI 和主机名校验。
- 资源边界：基础目标从 12 个收紧为 10 个，对比项上限 20，探测间隔最小 1 秒，并要求真实有界执行队列。
- 历史容量：120 个样本对 2 秒默认间隔偏短，调整为 180 个，约覆盖 6 分钟且内存仍有明确上限。
- 凭据安全：只保存不含密码的代理设置，密码保持在当前进程内并在清理时解除引用。
- 测试可重复性：移除对本机自签 TLS 证书设施的依赖，成功握手走公共冒烟验证，协议错误和资源清理由本机测试覆盖。

修订后的计划在功能覆盖、真实性、安全性和长期运行成本之间合理，可以进入实现阶段。

## 9. 执行结果与最终 Review

状态：已完成。

- 已按当前新项目基线完成 UDP/STUN、TCP Connect、TLS Handshake 统一监控，不读取或迁移旧 UDP 配置。
- 已完成系统路由、SOCKS5、HTTP CONNECT 以及系统路由/代理同节点对比；界面明确显示出口、本地源地址、网卡和代理端点。
- 已完成 RTT、峰值、抖动、失败率、连续失败、质量等级、趋势、公网映射与映射变化统计。
- 已落实零空闲轮询、单调度线程、最多 6 个工作线程、有界任务队列、单监控项单在途请求和每项 180 个样本上限。
- 已确认代理密码不写入配置、日志或错误信息，TLS 强制系统信任、SNI 与主机名校验。
- 最终 Review 修正了 SOCKS5 认证方法协商：配置凭据时同时声明无认证和用户名/密码认证，由代理按 RFC 1928 选择。
- 本机伪服务覆盖直接与代理协议路径；真实 Windows 系统路由完成 UDP/TCP/TLS 冒烟验证；JavaFX 桌面、最小窗口、对比模式和趋势折叠完成离屏视觉验证。
- 完整 Maven 测试与 `javafx:jlink` 打包通过，运行入口为 `target/app/bin/app.bat`。

构建时 JDK 23 会提示 JavaFX Maven 插件内部使用的旧式 `--compress 0` 参数未来将移除。当前插件版本未提供 `zip-N` 配置类型，该提示不影响本次运行镜像；后续升级打包插件时应同步切换新参数格式。
